<Window x:Class="CrmDemo.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход в CRM" Height="400" Width="500"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="CRM Система" FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,30"/>

        <StackPanel Grid.Row="1" Margin="0,10">
            <TextBlock Text="Логин" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Username}" Width="300" Height="30"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,10">
            <TextBlock Text="Пароль" Margin="0,0,0,5"/>
            <PasswordBox x:Name="PasswordBox" Width="300" Height="30"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" 
                    HorizontalAlignment="Center" Margin="0,20">
            <Button Content="Войти" Width="100" Height="35" Margin="5"
                    Command="{Binding LoginCommand}"/>
            <Button Content="Регистрация" Width="100" Height="35" Margin="5"
                    Command="{Binding RegisterCommand}"/>
        </StackPanel>

        <ProgressBar Grid.Row="4" IsIndeterminate="True" Height="5"
                     VerticalAlignment="Bottom" Margin="0,20"
                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
    </Grid>
</Window>